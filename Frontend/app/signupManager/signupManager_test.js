'use strict';

describe('myApp.signupManager module', function() {

  beforeEach(module('myApp.signupManager'));

  describe('signupManager controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var myWeeklyCtrl = $controller('signupManagerCtrl');
      expect(myWeeklyCtrl).toBeDefined();
    }));

  });
});
'use strict';

describe('myApp.searchCenter module', function() {

  beforeEach(module('myApp.searchCenter'));

  describe('searchCenter controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var searchCenterCtrl = $controller('searchCenterCtrl');
      expect(searchCenterCtrl).toBeDefined();
    }));

  });
});
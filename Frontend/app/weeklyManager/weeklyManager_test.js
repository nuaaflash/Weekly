'use strict';

describe('myApp.weeklyManager module', function() {

  beforeEach(module('myApp.weeklyManager'));

  describe('weeklyManager controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var weeklyManagerCtrl = $controller('weeklyManagerCtrl');
      expect(weeklyManagerCtrl).toBeDefined();
    }));

  });
});